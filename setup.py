from setuptools import setup, find_packages
setup(name='sub',
      version='1.0',
      description='Test',
      author='Me',
      author_email='me@example.com',
      license='MIT',
      packages=find_packages(),
      zip_safe=False)
